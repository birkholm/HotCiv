package variants.alphaCiv;

import common.ActionStrategy;
import common.City;
import common.Position;
import common.Unit;

import java.util.Map;

public class AlphaActionStrategy implements ActionStrategy {
    @Override
    public void performAction(Position p, Map<Position, Unit> unitMap, Map<Position, City> cityMap) {

    }
}
